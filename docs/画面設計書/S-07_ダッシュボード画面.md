# S-07 ダッシュボード画面

- 更新日: 2026-08-09
- 関連文書: [画面一覧](../画面一覧.md) / [backendリポジトリ docs/API設計書/GET_dashboard-summary.md](https://github.com/h-fujiwara-dev/ielts-creater-backend/blob/main/docs/API設計書/GET_dashboard-summary.md)

## 画面概要

学習履歴に基づくスコア推移・正答率の傾向を可視化する参照専用の画面。ログイン必須。

## 画面構成要素

| 要素 | 内容 |
| --- | --- |
| スコア推移グラフ | 日次等の時系列での正答率推移（`ScoreTrendChart.tsx`） |
| セクション別平均正答率 | Reading / Listening それぞれの平均正答率 |
| 出題形式別正答率 | TFNG / MCQ / FILL_BLANK / MATCHING_HEADINGS ごとの正答率 |
| 受験回数 | 総受験回数（`totalAttempts`） |

## 入力項目とバリデーション

| 項目 | 種別 | 必須 | バリデーション |
| --- | --- | --- | --- |
| 期間フィルタ | 単一選択 | 任意 | クライアント側での表示範囲切り替え（実装メモ参照） |
| セクションフィルタ | 単一選択 | 任意 | クライアント側での表示範囲切り替え（実装メモ参照） |

## 状態・画面遷移

| 操作/状態 | 遷移先・表示 | 条件・備考 |
| --- | --- | --- |
| 画面初期表示 | 集計データを表示 | `GET /api/v1/dashboard/summary`を呼び出す |
| 期間・セクションによる表示切り替え | 同一画面内で表示を更新 | 遷移なし（参照専用画面） |

## API/データ連携

| API | 用途 | 備考 |
| --- | --- | --- |
| `GET /api/v1/dashboard/summary` | ダッシュボード集計データ取得 | [API設計書](https://github.com/h-fujiwara-dev/ielts-creater-backend/blob/main/docs/API設計書/GET_dashboard-summary.md) |

## 実装メモ

- **未決事項**: 現行の`GET /api/v1/dashboard/summary`のAPI設計書には期間・セクションを絞り込むクエリパラメータの定義がなく、レスポンスは常に全期間・両セクションの集計を返す形になっている。「期間・セクションによる表示切り替え」（F-11）は、サーバー側でクエリパラメータを受け付けて絞り込むか、フロントエンド側で取得済みデータをクライアントサイドでフィルタリングするか、backend側と方針をすり合わせる必要がある
